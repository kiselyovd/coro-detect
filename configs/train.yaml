defaults:
  - data: default
  - model: main
  - _self_

seed: 42
optuna_trials: 50
cv_folds: 5

paths:
  processed_dir: data/processed
  artifacts_dir: artifacts
  reports_dir: reports

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
