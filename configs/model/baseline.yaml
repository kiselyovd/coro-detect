# RandomForest baseline defaults; short GridSearchCV grid overrides at train-time.
_target_: cardio_risk_rf.models.factory.build_baseline
random_state: 42
n_estimators: 500
max_depth: null
min_samples_split: 2
min_samples_leaf: 1
max_features: sqrt
